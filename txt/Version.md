# V 0.0.1
增加了基本的README文档，简要介绍了数据所需安装包

# V 0.0.8
完善了README文档，详细介绍了在过程中可能出现的各种问题，并给出一定解决方案
完成了基本的从数据输入到segment切割的部分代码，并改进了大量BUG，增加了众多防呆设计。
增加了手动选择marker段的部分，有助于进一步数据处理
优化了手动选择marker段的UI，便于操作
增加了Log功能，有助于排查BUG
增加了Function Document文档，便于查询各个函数含义
增加Version文档，便于进行版本控制

#  V 0.1.0
完成了片段切割功能
完成了README文档，详细介绍了在片段切割过程中可能出现的各种问题，并给出一定解决方案
留下适当的拓展接口
最后，完成BUG修复（修复了前序可能出现的问题）

# V 0.1.2
完成了坏通手动判别功能
完成了plot功能

# V 0.4.0
完成坏段自动识别、人工标记与统一切割流程，并将标记结果保存在`EEGdata.artifact`中
完成可配置的预处理Pipeline，包括重采样、去趋势、带通、工频滤波、重参考和缺失值处理
完成鲁棒估计、ASR自动处理、ICA人工成分排除及最终人工频域复核流程
人工复核支持通道切换、频率定位、可疑频段记录，以及剔除坏通道后从ICA前数据重新执行ICA
新增`EEGdata.Process`处理状态记录，并兼容坏段处理和数据切分阶段的历史数据
统一Pipeline日志开关、日志目录和命名规则，补充示例、函数文档、README与全流程操作说明

# V 0.4.2
最终频域人工复核新增“退回上一步，重新ICA”，可放弃当前未确认标记并重新选择ICA成分
新增`DataQualitySummary`与`QualityIndex_pipeline`，汇总数据在切分、坏段处理和预处理阶段的有效状态
质量结果统一保存在`EEGdata.quality`，包括坏导、通道—有效比例cell和`totalEffectiveRate`
新增合成单元测试、现有8CH数据集成测试、调用示例及对应流程文档

# V 0.5.0
新增SPM风格的统一工作流UI，可分别点击“先分段”或“先预处理”，并完整配置路径、阶段开关、坏段和预处理参数
新增连续优先流程：每份BDF在长连续数据上完成一次坏段处理、ASR和人工ICA，再按Marker计划切割清洗结果，避免每个小片段重复ICA
`Artifact_pipeline`新增BDF直接输入，`Preprocess_pipeline`新增BDF、分段MAT和坏段MAT输入类型选择
新增`Workflow_pipeline`、`WorkflowOptions`、`ArtifactOptions`、`EEGdataFromBDF`和`segment_EEGdata`
统一界面预留“统计分析（待开发）”禁用按钮；同步更新示例、README、函数文档、操作说明和自动测试

# V 0.5.1
修复坏段和预处理信号参数页下半部分被截断的问题，全部参数可在单页完整显示
修复人工坏段编辑器重复激活Figure导致的“参数必须为图窗对象或正整数”，并明确区分空表确认与取消文件
连续ICA默认最多使用100000个均匀采样点估计模型，再应用到完整数据；增加ICA计算等待窗口、日志进度和可配置训练点数
工作流只把本次坏段阶段实际生成的文件传入预处理；零输出时安全停止，避免误读旧文件
新增顶层`HyperEEG`启动器：单通道灰显待开发，多通道进入统一工作流

# V 0.5.2
流程、坏段、信号步骤、自动伪迹、人工复核和日志页面统一改为R2023a原生可滚动Grid，解决小窗口参数截断和隐藏页签宽度异常
新增运行态关闭保护：点击关闭或窗口关闭键先确认是否取消，确认后保留主UI和参数
新增`WorkflowCancel`协作式取消状态，并在Marker、分段、坏段、预处理、ICA和保存前加入安全停止检查点
运行中取消会关闭当前人工复核窗口；同步计算在当前计算块完成后停止，未完成文件不保存

# V 0.5.3
无输出调用`HyperEEG`时改为打印平台名称、版本、版权、运行环境和注意事项，不再显示`ans`结构体；显式接收输出时保持兼容
多通道工作流底部新增“打开操作说明”按钮，可直接打开完整流程TXT
修复先预处理完成复核后进入最终分段时，`getFiles`混用string和char导致`dir`报“文件夹名称必须为文本标量”
修复空字符串忽略名单被误判为已配置文件的问题
运行失败时界面日志记录完整MATLAB错误堆栈，便于继续排查

# V 0.5.4
将全流程TXT重排为正式PDF手册，保留全部操作细节并突出推荐流程、安全红线和人工复核原则
PDF新增单页可点击目录、文档书签、页眉页脚、页码、代码框和批次签核清单
多通道界面“打开操作说明”改为调用系统PDF阅读器，原TXT不再保留

# V 0.5.5
Marker界面的Sample index和Time (ms)改为完整整数显示，不再使用科学计数法，毫秒不保留小数
操作说明PDF移动到`txt`目录，并在统一工作流中同步更新打开路径
PDF新增启动器、流程路径、坏段参数、预处理参数和Marker分段界面的实际点击截图与操作说明
使用真实`001.bdf`完成先预处理与先分段两条入口回归测试，覆盖坏段、重采样、滤波、平均重参考、robust、ASR及最终分段
补充工作流阶段推进、成功文件白名单、空输出停止、原始Marker边界与派生数据切割等关键维护注释

# V 0.6.0
主工作流启用独立“统计分析”入口，新增输入/导出总控制、个体水平时域统计五类子版块和运行日志
实现参考说明5.1全部时域指标，并补充分位数、截尾均值、变异系数、能量和绝对值和；各指标可独立选择
新增按指标忽略、插值、填零、拒绝通道和拒绝文件等缺失策略，以及方差、线长和零交叉归一化参数
每个输入文件保存`_time_domain_statistics.mat`，全部结果同时导出为带Settings页的长格式XLSX
新增多人分组XLSX示例与七页`HyperEEG统计分析指标说明.pdf`，均可从统计界面打开

# V 0.7.0
统计控制台新增独立“个体水平：熵与非线性”设置页，分为熵、排列/SVD/LZ、Hurst/DFA/分形、多尺度/探索性和RQA五个子版块
实现5.3全部参考指标：频谱熵、差分熵、样本熵、近似熵、排列熵、Lempel-Ziv、Hurst、DFA、三种分形维数及12项RQA汇总
补充模糊熵、SVD熵、多尺度样本熵、相关维数和最大Lyapunov指数；后两项标为探索性并默认关闭
XLSX新增NonlinearScalar和NonlinearSeries：标量直接列出，尺度/半径/延迟曲线逐点长表保存；RQA矩阵默认仅存MAT，可选逐点导出
新增七页`HyperEEG熵与非线性指标说明.pdf`及非线性批处理示例，记录全部参数、质量字段和解释边界

# V 0.8.0
统计控制台新增独立“个体水平：频谱与频带”设置页，分为PSD/Welch、频带功率、频谱形状、1/f与周期峰、比值与FAA五个子版块
实现5.2全部参考输出：完整PSD、绝对/相对/对数频带功率、谱形标量、非周期指数/截距/拟合优度、周期峰列表、三类比值和额叶Alpha不对称
补充谱边缘频率SEF90/95、个体Alpha峰和频谱平坦度；支持五个默认频带、自定义频带、线噪声排除范围及FAA电极对/符号/变换配置
XLSX可完全关闭；非单值频谱结果可选择long长表、wide宽表、separate多个工作簿或none仅标量，并可设置XLSX频点步长；MAT始终保存完整数据
新增`FrequencyStatistics`核心、5.2批处理示例和`HyperEEG频谱与频带指标说明.pdf`，同步更新README与函数文档

# V 0.9.0
5.1计算与XLSX导出解耦，新增wide、long、both、none四种布局，并补充分位数插值、MAD定义、偏度/峰度偏差校正和零交叉参考中心算法选择
5.3新增Welch/Periodogram频谱熵、Gaussian/Histogram差分熵、模板熵标准化与Chebyshev/Euclidean距离、RQA距离选择
5.3标量支持wide/long/both/none；DFA、MSE、相关维数和Lyapunov序列支持long/wide/separate/none
RQA递归矩阵可导出递归点坐标或带RQA_Index的独立0/1稠密矩阵工作簿，坐标表可选择主工作簿或独立工作簿
MAT仍为完整真源；所有XLSX关闭或版块设为none时不影响计算和MAT保存。同步更新5.1/5.3 PDF、示例、README和函数文档

# V 0.9.1
统计控制台将总体页更名为“输入与总体控制”，移除PSD等指标专属导出参数；总体页只保留路径、统计表格总开关、计算版块和通用缺失策略
PSD导出格式与频点步长移动到“PSD与Welch”，周期峰新增独立导出格式并移动到“1/f与周期峰”；两类结果可分别选择long、wide、separate或none
界面、提示和文档中的“联立表格”统一更名为“统计表格”；旧`excel.nonScalarMode`仍兼容，新增`excel.psdMode`与`excel.periodicPeakMode`
原5.1详细指南更名为`HyperEEG个体水平时域统计指标说明.pdf`；`HyperEEG统计分析指标说明.pdf`保留原名并重制为5.1-5.3全部指标的简明总览
README、函数文档和全流程操作说明新增统计控制台、三类详细指南、算法选择与导出结构说明
