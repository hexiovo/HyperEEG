# V 0.0.1
增加了基本的README文档，简要介绍了数据所需安装包

# V 0.0.8
完善了README文档，详细介绍了在过程中可能出现的各种问题，并给出一定解决方案
完成了基本的从数据输入到segment切割的部分代码，并改进了大量BUG，增加了众多防呆设计。
增加了手动选择marker段的部分，有助于进一步数据处理
优化了手动选择marker段的UI，便于操作
增加了Log功能，有助于排查BUG
增加了Function Document文档，便于查询各个函数含义
增加Version文档，便于进行版本控制

#  V 0.1.0
完成了片段切割功能
完成了README文档，详细介绍了在片段切割过程中可能出现的各种问题，并给出一定解决方案
留下适当的拓展接口
最后，完成BUG修复（修复了前序可能出现的问题）

# V 0.1.2
完成了坏通手动判别功能
完成了plot功能

# V 0.4.0
完成坏段自动识别、人工标记与统一切割流程，并将标记结果保存在`EEGdata.artifact`中
完成可配置的预处理Pipeline，包括重采样、去趋势、带通、工频滤波、重参考和缺失值处理
完成鲁棒估计、ASR自动处理、ICA人工成分排除及最终人工频域复核流程
人工复核支持通道切换、频率定位、可疑频段记录，以及剔除坏通道后从ICA前数据重新执行ICA
新增`EEGdata.Process`处理状态记录，并兼容坏段处理和数据切分阶段的历史数据
统一Pipeline日志开关、日志目录和命名规则，补充示例、函数文档、README与全流程操作说明

# V 0.4.2
最终频域人工复核新增“退回上一步，重新ICA”，可放弃当前未确认标记并重新选择ICA成分
新增`DataQualitySummary`与`QualityIndex_pipeline`，汇总数据在切分、坏段处理和预处理阶段的有效状态
质量结果统一保存在`EEGdata.quality`，包括坏导、通道—有效比例cell和`totalEffectiveRate`
新增合成单元测试、现有8CH数据集成测试、调用示例及对应流程文档

# V 0.5.0
新增SPM风格的统一工作流UI，可分别点击“先分段”或“先预处理”，并完整配置路径、阶段开关、坏段和预处理参数
新增连续优先流程：每份BDF在长连续数据上完成一次坏段处理、ASR和人工ICA，再按Marker计划切割清洗结果，避免每个小片段重复ICA
`Artifact_pipeline`新增BDF直接输入，`Preprocess_pipeline`新增BDF、分段MAT和坏段MAT输入类型选择
新增`Workflow_pipeline`、`WorkflowOptions`、`ArtifactOptions`、`EEGdataFromBDF`和`segment_EEGdata`
统一界面预留“统计分析（待开发）”禁用按钮；同步更新示例、README、函数文档、操作说明和自动测试

# V 0.5.1
修复坏段和预处理信号参数页下半部分被截断的问题，全部参数可在单页完整显示
修复人工坏段编辑器重复激活Figure导致的“参数必须为图窗对象或正整数”，并明确区分空表确认与取消文件
连续ICA默认最多使用100000个均匀采样点估计模型，再应用到完整数据；增加ICA计算等待窗口、日志进度和可配置训练点数
工作流只把本次坏段阶段实际生成的文件传入预处理；零输出时安全停止，避免误读旧文件
新增顶层`HyperEEG`启动器：单通道灰显待开发，多通道进入统一工作流

# V 0.5.2
流程、坏段、信号步骤、自动伪迹、人工复核和日志页面统一改为R2023a原生可滚动Grid，解决小窗口参数截断和隐藏页签宽度异常
新增运行态关闭保护：点击关闭或窗口关闭键先确认是否取消，确认后保留主UI和参数
新增`WorkflowCancel`协作式取消状态，并在Marker、分段、坏段、预处理、ICA和保存前加入安全停止检查点
运行中取消会关闭当前人工复核窗口；同步计算在当前计算块完成后停止，未完成文件不保存

# V 0.5.3
无输出调用`HyperEEG`时改为打印平台名称、版本、版权、运行环境和注意事项，不再显示`ans`结构体；显式接收输出时保持兼容
多通道工作流底部新增“打开操作说明”按钮，可直接打开完整流程TXT
修复先预处理完成复核后进入最终分段时，`getFiles`混用string和char导致`dir`报“文件夹名称必须为文本标量”
修复空字符串忽略名单被误判为已配置文件的问题
运行失败时界面日志记录完整MATLAB错误堆栈，便于继续排查

# V 0.5.4
将全流程TXT重排为正式PDF手册，保留全部操作细节并突出推荐流程、安全红线和人工复核原则
PDF新增单页可点击目录、文档书签、页眉页脚、页码、代码框和批次签核清单
多通道界面“打开操作说明”改为调用系统PDF阅读器，原TXT不再保留
